# setup.cfg
[metadata]
name = pytest-check
description = Runs pytest before committing!
version = 0.0.1
author = Frexom
author_email = imfrexom@gmail.com

[options]
packages = find:

[options.entry_points]
console_scripts =
    pytest-check = pytest_check.main:main
